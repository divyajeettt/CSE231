cd

NAME
        cd - change the shell working directory

SYNOPSIS
        cd [OPTION(s)] DIR

DESCRIPTION
        Changes the current working directory of the shell to DIR. When no arguments are given, it defaults to the HOME directory.

SUPPORTED OPTIONS:
        -L
            force symbolic links to be followed

        -P
            use the physical directory structure without following symbolic links
