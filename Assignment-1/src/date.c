#include <time.h>
#include <stdio.h>
#include <stdlib.h>

///////////////////////////////////////////////////////////////////////////////////////////////////

void getDate() {
    time_t datetime;
    time(&datetime);
    printf("%s \n", ctime(&datetime));
}

///////////////////////////////////////////////////////////////////////////////////////////////////

int main(int argc, char *argv[]) {
    getDate();
    // printf("OH MA G INSIDE %s \n", argv[0]);
    return 0;
}